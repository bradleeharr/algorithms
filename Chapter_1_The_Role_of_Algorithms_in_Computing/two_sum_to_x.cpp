#include "pch.h"
#include "two_sum_to_x.h"

// Problem:
// Describe an algorithm that, given a set S of n integers and 
// another integer x, determines whether S contains two elements
// that sum to exactly x. 
// 
// Your algorithm should take O(nlogn) time in the worst case.

// two_sum_to_x 
// A : Array of n integers
// n : size of A
// x : sum of exactly x
int two_sum_to_x(int* A, int n, int x) {
	for (int i = 0; i < x; i++) {
	}
}